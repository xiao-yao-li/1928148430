# Blender 5.0.1 制作教程：《光子旅行》开场 30 秒

> **适用版本：** Blender 5.0.1（已切换为简体中文界面）  
> **目标片段：** 脚本开场 0 ~ 30 秒  
> **输出规格：** 1920×1080 / 25fps / MP4（H.264）  
> **大赛合规说明：** 本教程配合百度文心、字节跳动豆包 AI、科大讯飞星火等官方指定 AI 工具，所有素材自主创作，符合 2026 年中国大学生计算机设计大赛黑龙江省赛规则。

---

## 目录

1. [项目初始化与渲染设置](#一项目初始化与渲染设置)
2. [阶段一：全黑屏 + 光点脉动（0-3 秒）](#二阶段一全黑屏--光点脉动0-3-秒)
3. [阶段二：光点爆炸 + 第一人称光速飞行（3-8 秒）](#三阶段二光点爆炸--第一人称光速飞行3-8-秒)
4. [阶段三：光子减速聚焦（8-15 秒）](#四阶段三光子减速聚焦8-15-秒)
5. [阶段四：拉远 + 旅行路线图（15-30 秒）](#五阶段四拉远--旅行路线图15-30-秒)
6. [渲染输出与后期合成](#六渲染输出与后期合成)
7. [常见问题与技巧](#七常见问题与技巧)

---

## 一、项目初始化与渲染设置

### 1.1 新建项目

1. 打开 Blender 5.0.1，点击顶部菜单 **文件 → 新建 → 常规**。
2. 默认场景中会有一个立方体、灯光和摄像机，全选（`A`）后删除（`Delete`）清空场景。

### 1.2 渲染引擎选择

在右侧 **属性面板（Properties）** 中点击 **渲染属性**（摄像机图标）：

| 参数 | 设置值 | 说明 |
|------|--------|------|
| 渲染引擎 | **EEVEE Next** | 实时渲染，速度快；光效与 Cycles 接近 |
| 采样数（渲染）| 64 | 平衡质量与速度 |
| 辉光（Bloom）| ✅ 开启 | 让光点产生发光光晕效果 |
| 环境光遮蔽 | ✅ 开启 | 增强景深感 |

> 💡 **提示：** 若对最终画质要求更高，可切换为 **Cycles** 渲染引擎，但渲染时间会大幅增加。

### 1.3 输出设置

点击 **输出属性**（打印机图标）：

| 参数 | 设置值 |
|------|--------|
| 分辨率 X | 1920 |
| 分辨率 Y | 1080 |
| 帧率 | **25 fps** |
| 输出路径 | `//render/`（项目目录下的 render 文件夹）|
| 文件格式 | **FFmpeg 视频** |
| 编码格式 | H.264 |
| 输出质量 | 高质量 |

### 1.4 时间线设置

- 将 **帧范围** 设为 `1 ~ 750`（25fps × 30秒 = 750帧）。
- 快捷键：在时间线编辑器中直接修改右上角的 **结束帧** 数值框为 `750`。

---

## 二、阶段一：全黑屏 + 光点脉动（0-3 秒，帧 1~75）

> **效果目标：** 纯黑背景，正中心出现针尖大的暖白色光点，缓慢脉动 2 次。

### 2.1 设置纯黑背景

1. 点击右侧 **场景属性**（三角形图标）→ **世界（World）**。
2. 在 **世界属性** 中点击颜色色块，将颜色设为 **纯黑**（R:0 G:0 B:0）。
3. 确保 **渲染属性 → 胶片（Film）→ 透明** 关闭（使用纯黑背景）。

### 2.2 创建光点球体

1. 按 `Shift+A` → **网格 → UV 球体**，半径设为 **0.02 m**（极小球体）。
2. 按 `G` 不移动（保持在世界坐标原点）。
3. 创建新材质：
   - 在 **材质属性** 中点击 **+ 新建**。
   - 将材质名称改为 `光点_暖白`。
   - **表面** 改为 **发光（Emission）**。
   - 颜色设为 **暖白色**：R:1.0 G:0.95 B:0.85。
   - **强度** 设为 `50`。

### 2.3 添加体积光晕（让光点看起来像光源）

1. 选中球体，`Shift+A` → **灯光 → 点光源**，放置在球体同一位置（原点）。
2. 灯光属性：
   - 颜色：R:1.0 G:0.92 B:0.75（暖白）
   - 功率：`5 W`（初始微弱）
   - 半径：`0.01 m`

### 2.4 制作脉动动画（K帧）

光点需在 **帧1~75**（0~3秒）内脉动 2 次：

| 帧号 | 球体缩放 | 灯光功率 | 说明 |
|------|----------|----------|------|
| 1 | 0.001 | 0 | 初始不可见 |
| 13 | 1.0 | 50 | 第 1 次脉动峰值（约0.5s）|
| 25 | 0.3 | 10 | 收缩回落 |
| 38 | 1.0 | 50 | 第 2 次脉动峰值（约1.5s）|
| 50 | 0.3 | 10 | 再次收缩 |
| 75 | 2.0 | 200 | 开始膨胀，准备爆炸 |

**K 帧操作步骤：**

1. 将时间线拖到 **帧 1**。
2. 选中光点球体，按 `I` → **缩放** 插入关键帧。
3. 选中点光源，在灯光属性的 **功率** 数值上 **右键 → 插入单帧关键帧**。
4. 按上表依次移动到对应帧，修改参数后插入关键帧。
5. 在 **图形编辑器（Graph Editor）** 中将所有关键帧的插值改为 **正弦（Sine）**，使脉动更自然：
   - 选中所有关键帧（`A`），按 `T` → **正弦**。

---

## 三、阶段二：光点爆炸 + 第一人称光速飞行（3-8 秒，帧 76~200）

> **效果目标：** 光点爆炸，切换为第一人称视角，拖出 5 段不同颜色的光轨拖尾，每段掠过一个场景。

### 3.1 制作光点爆炸粒子效果

#### 3.1.1 添加粒子系统

1. 选中光点球体，在 **粒子属性**（蓝色粒子图标）中点击 **+**。
2. 修改粒子参数：

| 参数 | 值 |
|------|----|
| 类型 | 发射器（Emitter）|
| 开始帧 | 75 |
| 结束帧 | 80 |
| 寿命 | 30 |
| 数量 | 500 |
| 速度 → 法向 | 80 |
| 随机化 | 0.5 |
| 渲染 → 渲染方式 | 光晕（Halo）|
| 光晕尺寸 | 0.05 |

#### 3.1.2 添加力场（爆炸向外扩散）

1. `Shift+A` → **力场 → 爆炸（Explode）**，放置在原点。
2. 强度设为 `200`，衰减设为 `2.0`。

#### 3.1.3 爆炸时隐藏光点球体

- 帧 75：球体可见（在 **物体属性 → 可见性** 处打 K 帧，勾选可见）。
- 帧 76：球体不可见（取消勾选，插入关键帧）。

---

### 3.2 制作摄像机（第一人称视角）

1. `Shift+A` → **摄像机**，命名为 `摄像机_主视角`。
2. 将摄像机位置设为 `(0, -10, 0)`，旋转 `(90°, 0°, 0°)`，面向 Z 轴正方向。
3. 焦距设为 **18mm**（广角，增强速度感）。
4. 按 `Numpad 0` 切换到摄像机视角预览。

**第一人称视角动画：**  
让摄像机沿 Y 轴高速向前飞行：

| 帧号 | 摄像机 Y 坐标 | 说明 |
|------|--------------|------|
| 76 | -10 | 爆炸后开始飞行 |
| 200 | 500 | 向前飞出 510 个单位（模拟光速）|

**K帧方法：** 选中摄像机 → 帧76 → `I` → 位置 → 移到帧200 → 修改Y值为500 → `I` → 位置。

---

### 3.3 制作 5 段光轨拖尾（Curve + Shader）

光轨是视觉核心，使用 **贝塞尔曲线 + 发光材质 + 运动模糊** 实现。

#### 3.3.1 创建光轨曲线

1. `Shift+A` → **曲线 → 贝塞尔曲线**，命名为 `光轨_1_暖白到鎏金`。
2. 进入 **编辑模式**（`Tab`），将曲线沿 Y 轴拉伸，形成一条向前延伸的光带。
3. 在 **曲线属性** 中：
   - **几何体 → 深度**：`0.005`（控制光轨粗细）
   - **几何体 → 分辨率**：`12`

#### 3.3.2 为光轨 1 创建渐变发光材质（暖白 → 鎏金）

1. 新建材质，命名为 `光轨材质_1`。
2. 打开 **着色器编辑器（Shader Editor）**，删除默认节点，添加以下节点网络：

```
[纹理坐标] → [UV] → [渐变纹理(线性)] → [颜色映射]
                                          ↓
                              [发光(Emission)] → [材质输出]
```

**具体步骤：**

- `Shift+A` → **输入 → 纹理坐标**。
- `Shift+A` → **转换器 → 渐变纹理**，类型选 **线性（Linear）**。
- `Shift+A` → **颜色 → 混合颜色**（颜色1：暖白 `#FFF5E0`，颜色2：鎏金 `#FFD700`）。
- 连接到 **发光（Emission）** 节点，强度 `30`。
- 发光节点连接 **材质输出**。

#### 3.3.3 制作光轨出现的动画

光轨需要随摄像机前进逐渐 "划过"：

1. 选中光轨曲线，在帧 76 添加 **偏移量** 关键帧（曲线属性 → 几何体 → 偏移量 = -100）。
2. 在帧 105（约第1闪结束）添加关键帧（偏移量 = 100），使光轨快速划过。

#### 3.3.4 依次创建其余 4 段光轨

按下表创建并设置材质，每段光轨时长约 25 帧（1 秒）：

| 光轨编号 | 出现帧 | 消失帧 | 颜色渐变 | 场景提示 |
|----------|--------|--------|----------|----------|
| 光轨_1 | 76 | 101 | 暖白 `#FFF5E0` → 鎏金 `#FFD700` | 丁达尔森林光束 |
| 光轨_2 | 101 | 126 | 鎏金 `#FFD700` → 冰蓝 `#A8DFFF` | 湖面一分为二 |
| 光轨_3 | 126 | 151 | 冰蓝 `#A8DFFF` → 彩虹渐变 | 三棱镜彩虹 |
| 光轨_4 | 151 | 176 | 彩虹 → 红蓝偏振 `#FF3333`/`#3399FF` | 影院双画面 |
| 光轨_5 | 176 | 200 | 红蓝 → 激光紫 `#9900CC` | 钻入光纤 |

**快速复制方法：**
1. 选中光轨_1，`Shift+D` 复制，重命名。
2. 修改材质颜色和关键帧时间。

---

### 3.4 添加运动模糊增强速度感

1. **渲染属性 → 运动模糊** → ✅ 开启。
2. **快门** 设为 `0.5`（值越大模糊越强，此处适中）。

---

### 3.5 场景背景（每段光轨对应一个简单场景）

以 **光轨_1（丁达尔森林）** 为例：

1. `Shift+A` → **网格 → 平面**，放大 50 倍，作为地面。
2. 新建材质：颜色设为深绿 `#1A4A1A`，粗糙度 `0.8`。
3. `Shift+A` → **灯光 → 聚光灯（Spot）**，模拟从树冠射下的光柱：
   - 颜色：暖黄 `#FFE4A0`
   - 功率：`1000 W`
   - 聚光角度：`15°`
   - 启用 **体积散射**（需在 **世界属性** 中开启 **体积散射**，密度设为 `0.02`）

> **其余场景（湖面、三棱镜等）简化处理：** 可使用 Blender 内置的简单几何体搭建，或使用 AI 生成的图片作为环境贴图（HDRI），用豆包 AI 生成场景图后导入即可。

---

## 四、阶段三：光子减速聚焦（8-15 秒，帧 201~375）

> **效果目标：** 光子飞行速度放缓，画面聚焦在一颗清晰的光子上，周围环绕细碎光点。

### 4.1 摄像机减速动画

1. 将摄像机 Y 轴关键帧延伸：

| 帧号 | 摄像机 Y | 缓动 |
|------|----------|------|
| 200 | 500 | — |
| 250 | 520 | — |
| 375 | 530 | 缓出（Ease Out）|

2. 在 **图形编辑器** 中选中 200~375 帧的关键帧，按 `T` → **贝塞尔**，手动拖动手柄让速度曲线逐渐趋于平缓。

### 4.2 创建 "主光子" 物体

1. `Shift+A` → **网格 → UV 球体**，半径 `0.15`，命名为 `主光子`。
2. 材质：**发光**，颜色白色，强度 `80`。
3. 位置：摄像机正前方（摄像机 Y + 5），即大约 `(0, 535, 0)`。
4. 帧 200 时设置 **透明度为 0**（不可见），帧 250 时设置 **透明度为 1**（淡入显示）。

> **透明度淡入操作：**  
> 材质属性 → 混合模式选 **Alpha 混合** → 在 **Alpha** 通道上打关键帧。

### 4.3 创建周围碎光点（粒子系统）

1. 选中 `主光子`，添加粒子系统，命名 `光子环绕粒子`。
2. 参数设置：

| 参数 | 值 |
|------|----|
| 类型 | 发射器 |
| 开始帧 | 201 |
| 结束帧 | 375 |
| 寿命 | 50 |
| 数量 | 200 |
| 速度 → 法向 | 2 |
| 速度 → 随机化 | 1.0 |
| 重力 | 0（关闭重力）|
| 渲染 → 光晕 | 勾选 |
| 光晕尺寸 | 0.02 |
| 光晕颜色 | 白色，强度高 |

3. 添加 **轨道力场**：`Shift+A` → **力场 → 涡流（Vortex）**，放置在主光子中心，强度 `5`，使碎光点绕着主光子旋转。

### 4.4 景深效果（对焦光子）

1. 选中摄像机，在 **摄像机属性** 中开启 **景深（Depth of Field）**：
   - **焦点物体**：选择 `主光子`
   - **光圈（F-Stop）**：`1.4`（大光圈，背景虚化）
2. 在 **渲染属性 → EEVEE** 中开启 **景深（Depth of Field）**。

---

## 五、阶段四：拉远 + 旅行路线图（15-30 秒，帧 376~750）

> **效果目标：** 镜头拉远，出现完整的光子旅行路线图，6 个站点图标依次高亮。

### 5.1 摄像机拉远动画

| 帧号 | 摄像机位置 Z | 摄像机 Y | 说明 |
|------|------------|----------|------|
| 376 | 0 | 535 | 聚焦光子后 |
| 450 | 30 | 535 | 向上拉远 |
| 500 | 60 | 500 | 继续拉远 |
| 550 | 80 | 450 | 俯视视角 |

调整摄像机旋转，确保始终朝向场景中心。

### 5.2 创建旅行路线图

路线图使用 **曲线 + 文字 + 图标平面** 制作，位于一个新的空的场景层。

#### 5.2.1 绘制路线主曲线

1. `Shift+A` → **曲线 → 贝塞尔曲线**，命名 `旅行路线_主线`。
2. 在编辑模式中，将曲线塑造成从左到右的弯曲路径，代表光子从太阳到人眼的旅程。
3. 曲线属性 → **几何体 → 深度**：`0.02`（较细的发光路线）。
4. 材质：发光，颜色渐变（从橙黄到蓝白），强度 `20`。

#### 5.2.2 创建 6 个站点图标

对每个站点做如下操作（以"太阳/光子诞生"为例）：

1. `Shift+A` → **网格 → 平面**，缩小到 `1×1`，命名 `图标_1_光子诞生`。
2. 新建材质，在 **基础色** 中点击小圆点 → **图像纹理** → 加载图标图片。

   > 图标图片可用豆包 AI 生成：提示词示例 "一个发光太阳图标，扁平风格，白色线条，透明背景，PNG格式"。

3. 将 6 个图标沿路线曲线均匀排列。

| 站点编号 | 图标命名 | X 坐标 | 说明 |
|----------|----------|---------|------|
| 1 | 图标_1_光子诞生 | -8 | 太阳图标 |
| 2 | 图标_2_真空飞驰 | -5 | 光束/箭头 |
| 3 | 图标_3_湖面岔路 | -2 | 水波/折射 |
| 4 | 图标_4_三棱镜 | 1 | 棱镜彩虹 |
| 5 | 图标_5_影院偏振 | 4 | 3D 眼镜 |
| 6 | 图标_6_人眼 | 7 | 眼睛图标 |

#### 5.2.3 站点高亮动画

让图标依次放大 + 发光高亮：

| 图标 | 高亮开始帧 | 高亮结束帧 |
|------|-----------|-----------|
| 图标_1 | 500 | 560 |
| 图标_2 | 560 | 610 |
| 图标_3 | 610 | 650 |
| 图标_4 | 650 | 690 |
| 图标_5 | 690 | 720 |
| 图标_6 | 720 | 750 |

**高亮操作（以图标_1为例）：**

1. 帧 500：缩放 = 1.0，材质发光强度 = 1，插入关键帧。
2. 帧 530：缩放 = 1.5，材质发光强度 = 10，插入关键帧。
3. 帧 560：缩放 = 1.2，材质发光强度 = 5，插入关键帧。

#### 5.2.4 添加站点文字标签

1. `Shift+A` → **文字**，输入站点名称（如"光子诞生"）。
2. 字体推荐使用 **思源黑体**（免费开源字体，符合比赛版权要求）：
   - 文字属性 → **字体 → 常规** → 加载思源黑体 `.otf` 文件。
3. 将文字放置在对应图标上方，与图标同步高亮。

---

## 六、渲染输出与后期合成

### 6.1 渲染前检查清单

- [ ] 帧范围：1 ~ 750
- [ ] 分辨率：1920 × 1080
- [ ] 帧率：25 fps
- [ ] 渲染引擎：EEVEE Next
- [ ] 辉光（Bloom）：已开启
- [ ] 运动模糊：已开启，快门 0.5
- [ ] 输出格式：FFmpeg 视频 / H.264

### 6.2 分段渲染（节省时间）

建议分 4 段渲染，便于发现错误后局部修改：

| 段落 | 帧范围 | 预计时长 |
|------|--------|--------|
| 阶段一（黑屏+光点）| 1 ~ 75 | 约 5 min |
| 阶段二（爆炸+飞行）| 76 ~ 200 | 约 15 min |
| 阶段三（聚焦光子）| 201 ~ 375 | 约 20 min |
| 阶段四（路线图）| 376 ~ 750 | 约 30 min |

**渲染单段操作：**
1. 修改 **输出属性 → 帧范围**。
2. 顶部菜单 **渲染 → 渲染动画**（快捷键 `Ctrl+F12`）。

### 6.3 在 VSE 中合并片段

1. 顶部工作区选择 **视频剪辑（Video Editing）**。
2. 将 4 段渲染视频依次拖入 **视频序列编辑器（VSE）**，按顺序拼接。
3. **渲染 → 渲染动画** 输出最终的 30 秒合并视频。

### 6.4 添加旁白音频

1. 将录制好的旁白音频文件拖入 VSE 音频轨道。
2. 对齐到第 1 帧，调整音量。
3. 可在 VSE 中简单添加 **淡入淡出**（选中音频条 → 右键 → 添加淡入 / 淡出）。

---

## 七、常见问题与技巧

### Q1：粒子爆炸效果卡顿怎么办？

降低粒子数量（从 500 降到 200），或在 **粒子属性 → 渲染** 中将 "在渲染中显示" 开启，预览时关闭粒子显示（点击属性面板上方的眼睛图标）。

### Q2：发光效果在预览窗口看不到？

在 **3D 视口** 右上角的 **视口着色（Viewport Shading）** 切换为 **渲染预览（Rendered）** 模式（快捷键 `Z` → 渲染）。

### Q3：颜色渐变光轨颜色不对？

打开 **着色器编辑器**，在 **颜色渐变纹理** 节点上展开色带，手动拖动颜色停止点并设置颜色。

### Q4：如何快速对齐图标到曲线上？

1. 选中图标，`Shift+A` → **约束 → 跟随路径（Follow Path）**。
2. 目标选择 `旅行路线_主线` 曲线。
3. 调整 **偏移量** 数值将图标移动到曲线的对应位置。

### Q5：文件过大渲染速度慢？

- 使用 **视图层** 分层渲染，将粒子、场景、路线图分别放在不同视图层，单独渲染后在 VSE 合成。
- 渲染前压缩图标图片分辨率到 512×512。

---

## 附：大赛合规说明

| 项目 | 说明 |
|------|------|
| 使用 AI 工具 | 百度文心一言（文案润色）、字节跳动豆包 AI（图标生成）、科大讯飞星火（配音合成）—— 均为大赛官方指定工具 |
| 原创声明 | 所有 3D 模型、材质、动画均在 Blender 中自主创作，AI 工具仅辅助生成贴图素材和文案 |
| 字体版权 | 使用思源黑体（SIL 开源协议）或方正字体（需获取免费授权），不使用未授权商业字体 |
| 音乐版权 | 使用 **Pixabay** 或 **FreePD** 上的免费商用音效，或自行录制 |
| 素材存档 | 所有 AI 生成素材截图保存，作为作品说明材料备用 |

---

> **祝制作顺利！** 完成 30 秒开场后，可参考脚本继续制作后续各站点片段。每个站点的核心效果（反射折射、色散、全反射等）均可用类似的 Blender 节点与动画方法实现。
